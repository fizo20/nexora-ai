// src/services/workspace-plan.service.ts
/**
 * Stub — replace with billing integration later
 */
export const getWorkspacePlan = async (
  workspaceId: string,
): Promise<"FREE" | "PRO" | "ENTERPRISE"> => {
  return "FREE";
};
